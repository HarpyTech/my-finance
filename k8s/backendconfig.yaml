apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: ${BACKEND_CONFIG_NAME}
spec:
  timeoutSec: 60
  healthCheck:
    type: HTTP
    requestPath: /health
    port: 8000
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 3
