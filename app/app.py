# This file is a legacy prototype and is NOT used by the application.
# The production extraction logic lives in:
#   app/services/expense_extraction_service.py
# and is exposed via:
#   app/api/routes/expenses.py  (POST /expenses/extract-and-create)
